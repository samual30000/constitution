"""
================================================================================
【宪法：设计理念和原则】
================================================================================

⚠️ 本文件包含商业秘密，严禁外泄、复制或用于其他项目
⚠️ 仅限授权团队成员访问，违者将追究法律责任
⚠️ Copyright (c) 2025 [你的名字/公司]. All Rights Reserved.

================================================================================

⚠️ 警告：看不懂宪法的禁止修改任何代码

核心理念：鼓励大量拆分，但避免逻辑撕裂

1. 逻辑完整性原则（拆分的边界）
   - 一个函数必须是一个完整的业务逻辑单元
   - 拆分后的函数能否独立使用？如果不能，说明逻辑被撕裂了
   - 过度拆分其实是逻辑撕裂

2. 动词-名词原则
   - 函数名必须是"动词+名词"的形式
   - 例如：scan_missing_characters（扫描缺失角色）
   - 例如：complete_characters（补全角色）
   - 例如：get_characters（获取角色）
   - 例如：add_character（添加角色）

3. 重复代码原则：3处以上相同逻辑才抽取
   - 不要看到2处重复就立即抽取
   - 等到3处以上重复时再考虑抽取
   - 避免过早抽象

4. 自然边界原则
   - 按照业务的自然边界拆分
   - 不要为了拆分而拆分

5. 可测试性原则
   - 拆分后的函数应该容易测试
   - 如果难以测试，说明拆分不合理

【拆分的黄金标准】
问自己：拆分后的函数能否独立使用？
- 能 → 合理拆分
- 不能 → 逻辑撕裂，不要拆分

【合约强制执行】
所有实现类必须：
1. 只能实现合约中定义的公开方法
2. 内部辅助方法必须使用 _ 前缀（私有方法）
3. 使用 whitelist_enforcer.py 自动生成白名单 


【宪法引用规则】
contract作为保留字只存放极简抽象类

1. 极简抽象类（contracts.py）必须引用宪法,禁止传递任何参数,核心是极简.
   - 在文件顶部添加：from model.constitution import CONSTITUTION_TEXT
   - 注释：# 没有阅读宪法之前 AI禁止修改代码

2. 具体实现类（base.py）必须引用宪法
   - 在文件顶部添加：from model.constitution import CONSTITUTION_TEXT
   - 注释：# 没看懂宪法之前,禁止修改任何代码

3. 业务实现类不需要引用宪法
   - 它们继承自 base.py，自动受宪法约束

【命名规范】
1. 文件命名：
   - contracts.py：极简抽象类（只有方法名和一行注释）
   - 无论私有函数还是 公共函数都必须在极简抽象类中定义
   - base.py：详细抽象类（完整的方法签名和文档）
   - 具体实现：按功能命名（如 handler.py, validator.py）

2. 类命名：
   - 接口类：I + 功能名 + 类型（如 ICharacterCompletionRoutes）
   - 抽象类：Abstract + 功能名 + Base（如 AbstractPromptEnhancerBase）
   - 具体类：功能名 + 类型（如 UsernameHandler）

3. 方法命名：
   - 必须遵循"动词+名词"格式
   - 使用下划线分隔（snake_case）
   - 例如：check_missing_characters, complete_missing_characters

【AI 协作原则】
1. AI 禁止修改未理解宪法的代码
2. AI 必须遵循宪法中的所有设计原则
3. 如果没有用户反复确认,禁止使用绝对路劲
4. md文档统一放docs,禁止放根目录,禁止放代码目录 
5.宪法模块如果没有反复跟用户确认禁止被旧代码调用,一旦调用后患无穷.
6.宪法模块默认必须使用宪法路由
7.新旧模块禁止穿插调用,除非用户反复3次确认
8.禁止路由添加任何功能,功能必须放在model单独目录下
9.model当前目录制只允许产生目录,禁止直接产生文件

【AI 宪法目的】
1. 避免ai乱改加多余函数避免逻辑无端膨胀
2. 方便人类浏览函数名字就知道ai改了什么,一目了然,阅读成本最小化.



================================================================================
"""